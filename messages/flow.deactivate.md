# summary

Deactivate Flow definitions

# flags.name.summary

Flow API name. Specify the flag multiple times to deactivate multiple Flows.

# examples

- <%= config.bin %> <%= command.id %> --target-org my-org --name Flow1 --name Flow2
